---
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const iconSrc = `${base}/images/avatar.png`;
---

<link rel="icon" type="image/png" href={iconSrc} />
<link rel="apple-touch-icon" href={iconSrc} />
